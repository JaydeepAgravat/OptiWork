const colors = {
    light: {
        textPrimary: "#101828",
        textSecondary: "#475467",
        white: "#ffffff"
    }
}

export default colors;

export type ColorTokens = keyof typeof colors.light;